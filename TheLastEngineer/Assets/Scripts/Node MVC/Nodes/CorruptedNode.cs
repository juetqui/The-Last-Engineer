using UnityEngine;
using static TreeEditor.TreeEditorHelper;

public class CorruptedNode : NodeController
{
    private void Awake()
    {
        OnAwake();
    }

    void Start()
    {
        OnStart();
    }

    void Update()
    {
        OnUpdate();
    }
}
