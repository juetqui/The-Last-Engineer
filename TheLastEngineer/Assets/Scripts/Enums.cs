public enum PlayerState
{
    Empty,
    Grab,
    Place
}

public enum NodeType
{
    None,
    Default,
    Corrupted
}

public enum InteractablePriority
{
    Low = 0,
    Medium = 1,
    High = 2,
    Highest = 3
}
