public enum PlayerState
{
    Default,
    Corrupted
}

public enum NodeType
{
    None,
    Default,
    Corrupted
}
public enum StopType
{
    None,
    TimeStop,
    CorruptedStop
}

public enum InteractablePriority
{
    Low = 0,
    Medium = 1,
    High = 2,
    Highest = 3,
    MaxPriority = 4,
}
